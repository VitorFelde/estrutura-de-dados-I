#include <stdio.h>
#include <time.h>

int gerar_id() { 
    return (int)time(NULL);  //transforma em int para melhor controle dos ids
}

void gera_arquivo_alunos() {
    //depois implementaremos na main, por enquanto fica vazio para inicializar vazio
}
